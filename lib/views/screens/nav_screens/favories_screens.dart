import 'package:flutter/material.dart';

class FavoriesScreens extends StatelessWidget {
  const FavoriesScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Favories Screens'),
      ),
    );
  }
}